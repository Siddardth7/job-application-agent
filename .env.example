# Supabase Configuration (Optional: Used for centralized multi-device job application tracking)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-anon-or-service-role-key

# Apify Configuration (Optional: Used for automated LinkedIn scraping in tools/fetch_jobs.py)
# If left blank, tools/fetch_jobs.py uses free, zero-token direct ATS board scanning.
APIFY_TOKEN=your-apify-token-here

# Local Paths & Environment overrides (Optional)
# PATH=/Library/TeX/texbin:/usr/local/bin:$PATH
