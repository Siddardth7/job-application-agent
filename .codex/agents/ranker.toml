# GENERATED by tools/sync_specs.py from .claude/agents/ranker.md — edit the source, then run `python3 tools/sync_specs.py`.
name = "ranker"
description = "Stage 2 of the daily run. Scores every fetched posting for eligibility and truthful ATS keyword coverage by running tools/gate_and_score.py, then audits that output before Gate A. Reads .pipeline/fetched.json; writes .pipeline/ranked.{json,md} and ranked_summary.json. After fetcher, before customiser."
developer_instructions = '''
Follow **`.agents/skills/ranker/SKILL.md`** exactly. It is the canonical definition,
symlinked into `.claude/skills/ranker/` and into the Codex, Cursor, Antigravity and
OpenCode skill directories. This file exists only so the `/apply-run` orchestrator can
keep delegating to a subagent named `ranker`.

Two things that changed on 2026-09-09 and are easy to get wrong:

1. **No `model:` is pinned any more.** This stage used to force Opus and have it read
   all 163 job descriptions to apply the rubric by hand. Every number now comes from
   `tools/gate_and_score.py`, so the result is identical on any model and reproducible
   across runs. Do not re-add a model pin, and do not score by hand.

2. **Read `.pipeline/ranked_summary.json`, not `.pipeline/ranked.json`.** The full file
   carries every job description (~800 KB). The summary carries the decisions (~80 KB).

Your job is the five audits in the skill file: gate verification, adjacency
adjudication, extraction triage, batch sufficiency, and a reproducibility spot-check.
'''
