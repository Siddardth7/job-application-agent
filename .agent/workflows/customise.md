---
description: Tailor one track resume for a single hand-found role (standalone — no full apply-run). Delegates to the customiser subagent.
---
<!-- GENERATED by tools/sync_specs.py from .claude/commands/customise.md — edit the source, then run `python3 tools/sync_specs.py`. -->
In this tool, "delegate to the <name> subagent" means: open `.claude/agents/<name>.md` (same text as `.codex/agents/<name>.toml`) and carry out that role inline, one stage at a time. `$ARGUMENTS` is whatever the user typed after the command.

Tailor a resume for ONE role: $ARGUMENTS

Standalone entry to the `customiser` subagent — use when you find a role yourself and just want
the resume, outside `/apply-run`.

1. Get the role facts from `$ARGUMENTS` (company, title, JD text or URL, and track if known). If any
   are missing, ask you — do not guess a JD.
2. If the track isn't given, infer it per `playbook/P1_06_scoring_rubric.md` §2.5
   (T1 / T2 / T3 — a label that steers emphasis; all tracks tailor the same master template).
3. Delegate to the **customiser** subagent for this single role, telling it to use these facts as its
   input **instead of `.pipeline/ranked.md`**, and to write the tailored resume to
   `Job_Applications_Resumes/<today>/` per `playbook/P1_03_resume_customization_rules.md`.
4. Relay the `.tex`/`.pdf` path and the P1_03 3-line change summary. Do not apply or send — you applies.
