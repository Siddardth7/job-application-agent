<!-- GENERATED by tools/sync_specs.py from .claude/commands + AGENTS.md — edit the source, then run `python3 tools/sync_specs.py`. -->
---
trigger: always_on
---
This workspace is the job-application pipeline. Read `AGENTS.md` first, then `DAILY_RUN.md` — they are the authoritative sequence and guardrails. Run stages one at a time, never in parallel, and stop at Gate A and Gate B. Never apply to a job or send a message: the candidate does every submission. Supabase is the record for applications and the seen-job ledger; the tracker (`job_tracker.html`) reads it live, nothing deploys it. Workflows: `/apply-run`, `/fetch`, `/rank`, `/customise`, `/find-contacts`, `/interview`.
